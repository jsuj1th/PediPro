server {
    listen 3000;
    root /var/www/patient;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 3001;
    root /var/www/admin;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
